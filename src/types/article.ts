type ArticleType = {
  id: number;
  title: string;
  description: string;
  isDraft: boolean;
  content: string;
  handle: string;
  authorId: number;
  source: number;
};

export default ArticleType;
